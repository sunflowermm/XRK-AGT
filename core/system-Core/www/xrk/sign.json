{
  "id": "xrk",
  "name": "XRK-AGT 控制台",
  "enabled": false,
  "serve": "static",
  "staticRoot": "dist",
  "buildOnStart": false,
  "build": { "command": "pnpm", "args": ["build"] },
  "_note": "挂载阶段不 build。启动前由 Bootstrap 按需编（或 pnpm run build:www）。跳过：XRK_SKIP_WWW_BUILD=1。开发 HMR：enabled=true + serve=proxy。",
  "command": "pnpm",
  "args": ["dev"],
  "port": 5177,
  "proxy": { "mount": "/xrk" },
  "env": { "BROWSER": "none" },
  "autoRestart": true
}
