{
  "id": "xrk",
  "name": "XRK-AGT 控制台",
  "enabled": false,
  "serve": "static",
  "staticRoot": "dist",
  "build": { "command": "pnpm", "args": ["build"] },
  "_note": "enabled=false：产物过期或缺 dist 才 build。强制每次：buildOnStart true。跳过：false。开发 HMR：enabled=true + serve=proxy。",
  "command": "pnpm",
  "args": ["dev"],
  "port": 5177,
  "proxy": { "mount": "/xrk" },
  "env": { "BROWSER": "none" },
  "autoRestart": true
}
