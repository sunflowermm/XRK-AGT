{
  "id": "xrk",
  "name": "XRK-AGT 控制台",
  "enabled": false,
  "serve": "static",
  "staticRoot": "dist",
  "build": { "command": "pnpm", "args": ["build"] },
  "_note": "约定：维护者改 src 后建议 pnpm build 并把 dist 提交入库；也支持用户/部署方自行 build 或启动 stale 编。挂载不 build；编失败仍挂已有 dist。跳过：XRK_SKIP_WWW_BUILD=1。开发 HMR：enabled=true + serve=proxy。",
  "command": "pnpm",
  "args": ["dev"],
  "port": 5177,
  "proxy": { "mount": "/xrk" },
  "env": { "BROWSER": "none" },
  "autoRestart": true
}
