# 工作区业务 Core

种子 → `data/ai-workspace/{id}/core/`。Loader 一并扫描。

- **写**：`workspace-Core/{plugin,http,…}`（仅本工作区）
- **读框架**：`../../../.cursor/skills` · `docs` · `core/system-Core`（只读）
- 手册：**agent-core-dev**（含硬边界 + 了解项目清单 + `#` 导入）
