---
description: 工作区写 Core——边界 + 最短通用骨架（每轮注入；细则读 agent-core-dev）
alwaysApply: true
---

# 工作区写 Core

## 边界

- **写**：仅本工作区 `core/workspace-Core/`（plugin / http / workflow / …）。
- **读**：可读 `../../../.cursor` / `docs` / 仓库示例；禁止写回去。
- 用户已说「写/加一个」→ **直接 write**；说「继续」后禁止重读同一批文档。
- **禁止**为常见需求去翻 `src/`、Tasker；字段/其它扩展点 read **一次** **agent-core-dev**。

## 最短骨架（message）

路径：`core/workspace-Core/plugin/<名>.js`

```js
export class MyCmd extends PluginBase {
  constructor() {
    super({
      name: '我的命令', dsc: '#我的命令', event: 'message', priority: 5000,
      rule: [{ reg: '^#我的命令$', fnc: 'run' }],
    });
  }
  async run() { await this.reply('ok'); }
}
```

## 选型（勿偏一种玩法）

| 需求类型 | 落点 | 要点 |
|----------|------|------|
| `#命令` / 聊天关键词 | `plugin` + `event: 'message'` + `rule` | 上表骨架 |
| 通知类（进群、戳一戳、名片赞等） | `plugin` + `event: 'notice…'` | 逻辑多在 **`accept()`**；`rule` 可空（notice 常无 `e.msg`） |
| HTTP 接口 | `http/*.js` | `HttpResponse`；见 agent-core-dev §2 |
| 自定义 MCP 工具面 | `workflow/*.js` | AiWorkflow；少用 |
| 配置面板字段 | `commonconfig/*.js` | ConfigBase |

导入：裸名 `PluginBase` / `msgSegment`；HTTP → `#utils/http-utils.js`。  
**完整基类字段清单**：read **agent-core-dev**（Plugin / HTTP / workflow / events / config），勿翻 `src/`。
