---
description: XRK 仓库专属边界与 skill 入口（面向克隆本仓的 Coding Agent）
alwaysApply: true
---

# XRK Agent 补充

> 本仓开发入口：仓库根 `AGENTS.md`  
> 办事助手（产品 Agent）：`docs/agents.md` · `agents/` · `core/<core>/AGENTS.md`（若有）

## 本仓库边界

- 业务：`core/<core名>/`（plugin / http / workflow / tasker / events / commonconfig / www）
- Runtime：`src/infrastructure/` · `src/utils/` · `src/factory/`（框架维护）
- 独立 Core 配置：`core/<名>/default/` + `commonconfig/` + `data/<产品>/`
- 写 Core / Node API：`xrk-node-runtime`；HTTP：`HttpResponse`；全局裸名（`xrk-dev-requirements.mdc`）
- 写 / 审 `core/*/www`：`xrk-www-compat`
- 娱乐插件：配置写插件顶部，本地忽略；见 `xrk-project.mdc`
- 复用 Loader / ConfigBase；缺能力再扩 `src/`

## 本仓库 Skill 入口

| 主题 | Skill / 文档 |
|------|----------------|
| 开发总入口 | 根 `AGENTS.md`、`.cursor/skills/SKILL_INDEX.md` |
| 架构与放码 | `xrk-project`（rules）、`xrk-project-overview` |
| 外部调研 | `xrk-github-research` |
| 编码与性能 | `xrk-coding-style`、`docs/coding-style.md` |
| Core www / WebView | `xrk-www-compat`、`docs/www-mount.md` |
| HTTP API / 响应形状 | `xrk-http-api`、`docs/http-api.md` |
| 扩展点 | `xrk-infrastructure`、`docs/base-classes.md` |
| 办事助手 | `docs/agents.md` |

改 `core/` / `src/` 前：Grep + 读对应 `xrk-*`。外部方案说明如何接入 Loader / ConfigBase。  
独立 git 产品 Core 的精工约定见该 Core 的 `.cursor/rules/`（`xrk-project`「独立 git 产品 Core」）。  
文档只写现行契约。
