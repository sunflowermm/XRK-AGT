---
description: 主仓入库边界（core / data / 子服插件）
alwaysApply: true
---

# 主仓 gitignore 约定

根 `.gitignore` 只表达**什么进主仓库**，按目录用 `*` 忽略 + **框架白名单**，勿为每个产品单独加一行。

## core/

```
core/*
!core/system-Core/
!core/system-Core/**
```

- 主仓仅 `system-Core`；其余业务 Core（jm-Core、lsy-Core 等）本地存在即可，**勿**写 `!core/<产品>/` 或 `core/<产品>/`。

## data/

- 由 `data/.gitignore`（`*` + `!instruct.txt`）统一处理；根目录 **勿**重复列 `data/<产品>/` 或子路径。

## subserver/pyserver/apis/

```
subserver/pyserver/apis/*/
!subserver/pyserver/apis/system/**
!subserver/pyserver/apis/media-tools/**
!subserver/pyserver/apis/doc-pipeline/**
!subserver/pyserver/apis/web-fetch/**
```

- 框架示例外均为本地 clone（含 jmcomic 等第三方），**勿**写 `apis/jmcomic/` 等按名排除。
- 第三方插件不属于 XRK 底层：勿写进框架帮助、示例、注释、`PyserverApi` 封装。

## 子服运维

- 子服命令在 **子服终端 `子服>`** 输入，不经主服 stdin 转发。
